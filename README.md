# MS-vibecoding

Inventory management demo for a company selling Mac Minis for OpenClaw use.

## Run locally

1. Install dependencies:

	npm install

2. Start the app:

	npm start

3. Open:

	http://localhost:3000

The first run seeds `inventory.db` with Mac Mini models, OpenClaw bundles, accessories, storage, networking, and service SKUs.
